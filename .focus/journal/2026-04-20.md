# Journal — 2026-04-20

## — Focus v2 rebuild (LARGE, in progress)

- **Task:** Rework Focus based on Anthropic's harness-design article + cross-analysis of BMAD / agent-kernel / GSD / spec-kit / superpowers.
- **Status:** In progress — steps 1–4 of 6 done.
- **Steps completed:**
  - **Step 1 — Atomic task schema:** Every plan task now requires Files / Action / Verify / Done when / Commit. `[NEEDS CLARIFICATION]` is an execution blocker. Plan Self-Review expanded from 6 to 9 checks.
  - **Step 2 — Evaluator gate:** New `/focus:evaluate` slash command + `scripts/evaluator-brief.sh`. MEDIUM runs once; LARGE after each top-level task + before merge. Two invocation modes (sub-agent for Claude Code/Cursor, brief for Codex/Gemini/OpenCode).
  - **Step 3 — Handoff protocol:** Replaced the weak "50-tool-call warning" with a structured `## Handoff` block in plan.md. New `/focus:handoff` command, `scripts/session-context.sh`, `scripts/plan-tail.sh`. Hooks moved from inline shell to scripts. `check-complete.sh` flags handoffs missing `Exact next action:`.
  - **Step 4 — Memory split:** memory.md (mutable state) vs journal/YYYY-MM-DD.md (immutable narrative). Legacy `## Last Session` migrated; session-context surfaces Principles + Open Items + recent journal instead.
- **Files touched:** `skills/focus/SKILL.md`, `skills/focus/scripts/{check-complete,evaluator-brief,session-context,plan-tail,install}.sh`, `commands/{evaluate,handoff,status}.md`, `.focus/memory.md`, `.focus/journal/2026-03-26.md`.
- **Key decisions** (also in memory.md):
  - Handoff lives at bottom of plan.md (not a separate file) — avoids stale artifacts and single-source-of-truth wins.
  - One journal file per UTC date — balances session boundaries (slippery) with file count (bounded).
  - Principles go in memory.md under their own section, not a separate principles.md yet (kept optional for Step 5).
- **Notes for next session:** Steps 5 (principles gate) and 6 (SKILL.md modularization) remain. SKILL.md has grown from 448 → ~620 lines; step 6 will bring it back down by moving templates and protocol bodies to loadable files.
