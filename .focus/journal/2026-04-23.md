# Journal — 2026-04-23

## — v2 completion, install, namespace fix

- **Task:** Finish v2 rebuild (steps 5 + 6), push to GitHub, install + first use. (LARGE continuation)
- **Status:** Complete. v2 shipped as 6 stepwise commits on main, plus a follow-up SMALL fix.
- **Files:** all v2 files (SKILL.md, 5 scripts, 3 commands, 4 references, README, memory/journal split); then `commands/focus/{evaluate,handoff,status}.md` moved into subdir + `install.sh` updated.
- **Key decisions:**
  - Commands must live under `commands/focus/` — Claude Code derives slash names from filename paths, not YAML `name:` frontmatter. A flat `commands/evaluate.md` becomes `/evaluate`, which collides with other users' commands. Subdirectory is the only way to get `/focus:evaluate`.
  - Installer now removes legacy un-namespaced copies on upgrade, to prevent both `/<name>` and `/focus:<name>` resolving at once.
  - Cursor command install was missing in v2 entirely — added, same pattern.
- **Notes / honest retrospective:**
  - v2 itself was built without running through the v2 pipeline. No `.focus/plan.md` was created, no atomic task schema, no evaluator gate. The session audited itself mid-way and flagged this.
  - Memory.md Project Context is still stale — claims `hooks/` dir exists. Queued as next SMALL fix.
  - `~/.agents/skills/` is present on this machine (from other skills) but the installer never writes to it — just prints a hint about `.codex/INSTALL.md`. Real installer bug, escalated to MEDIUM-sized follow-up.
  - Next step: run `/focus:evaluate` retroactively against the v2 diff to dog-food the evaluator on its own creation.

## Retro: Focus v2 rebuild (2026-04-20 → 2026-04-23)

- **What went well:**
  - Cross-analysis of 5 reference harnesses produced a coherent 6-step design where each step had a specific theory of value, not just feature-creep.
  - Dog-food moments mid-work (declaring `MUST keep SKILL.md under 500 lines` and then exceeding it, forcing Step 6) validated that principles actually bite.
  - End-to-end sandbox tests for every script caught real bugs (awk range for Principles extraction; `-x` vs `-f` on unexecutable repo files).
- **What went poorly:**
  - Built the evaluator without ever running it. The single most important v2 feature got zero integration testing against a real LARGE task.
  - Skipped the clarifying-questions + design-options phase. The user repeatedly said "yes continue" which was read as green light, but Focus rules require structured presentation-and-approval before LARGE work.
  - Committed in one flat push at end-of-session instead of per-task during work. Lost the git-bisect story.
- **Change for next time:** any LARGE change to Focus itself MUST go through `.focus/plan.md` with atomic tasks. The builder has to eat their own cooking.
